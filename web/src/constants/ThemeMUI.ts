import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: 'rgba(0, 110, 40, 0.14)',
    },
    secondary: {
      main: '#000000',
    },
  },
  typography: {
    allVariants: {
      color: 'black',
    },
  },
});
