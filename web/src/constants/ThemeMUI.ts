import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#247922',
    },
    secondary: {
      main: '#000000',
    },
  },
  typography: {
    allVariants: {
      color: 'black',
    },
  },
});
